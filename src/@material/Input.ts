import Input from '@material-ui/core/Input';
import {withStyles} from '@material-ui/core/styles';

export const DefaultInput = withStyles(() => ({
    root: {
        /*fontSize: '16px',*/
    },
}))(Input);
